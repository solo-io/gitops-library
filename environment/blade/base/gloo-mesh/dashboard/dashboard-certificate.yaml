apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard
  namespace: istio-system
spec:
  secretName: dashboard
  commonName: dashboard.jad.solo.io
  dnsNames: 
    - dashboard.jad.solo.io
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
