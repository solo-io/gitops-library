apiVersion: networking.mesh.gloo.solo.io/v1
kind: TrafficPolicy
metadata:
  namespace: gloo-mesh
  name: argo-tls-disable
spec:
  sourceSelector:
  - kubeWorkloadMatcher:
      namespaces:
      - istio-system
  destinationSelector:
  - kubeServiceRefs:
      services:
        - clusterName: jad-mgmt
          name: argocd-server
          namespace: argocd
  policy:
    mtls:
      istio:
        tlsMode: DISABLE